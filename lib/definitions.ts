export type Categories = {
    id: number,
    name: string,
    description: string
}

export type Product = {
    id: number,
    name: string,
    description: string,
    price: number,
    stock_quantity: number,
    image_url: string,
}
